<Window
    x:Class="Editor.AboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    Title="About Math Editor"
    Width="436"
    Height="262"
    ui:TitleBar.Height="36"
    ui:WindowHelper.SystemBackdropType="Mica"
    ui:WindowHelper.UseModernWindowStyle="True"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    WindowStyle="SingleBorderWindow">
    <Grid Name="MainGrid" Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Width="80">
            <Image
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                RenderOptions.BitmapScalingMode="HighQuality"
                Source="/images/icon.png" />
        </Grid>
        <Label
            Name="versionLabel"
            Grid.Column="1"
            VerticalAlignment="Center"
            Content="Math Editor v.1.0.0.0"
            FontFamily="Arial"
            FontSize="14" />
        <Grid
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Width="{Binding ElementName=MainGrid, Path=ActualWidth}">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Label
                    VerticalAlignment="Center"
                    Content="Math Editor is a product from the&#160;"
                    FontFamily="Arial" />
                <ui:HyperlinkButton
                    Padding="0"
                    VerticalAlignment="Center"
                    Content="MathiVersity"
                    FontFamily="Arial"
                    NavigateUri="http://www.MathiVersity.com" />
                <Label
                    VerticalAlignment="Center"
                    Content="&#160;Project"
                    FontFamily="Arial" />
            </StackPanel>
        </Grid>
        <Label
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Margin="0 15 0 0"
            HorizontalAlignment="Left"
            Content="Copyright &#169; MathiVersity 2012-2013." />
    </Grid>
</Window>